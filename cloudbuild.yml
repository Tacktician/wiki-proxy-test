steps:
  # build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/wiki-proxy:$COMMIT_SHA', '.' ]
    # push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'push', 'gcr.io/$PROJECT_ID/wiki-proxy:$COMMIT_SHA']
    # deploy to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args: [
        'run', 'deploy', 'wiki-proxy',
        '--image', 'gcr.io/$PROJECT_ID/wiki-proxy:$COMMIT_SHA',
        '--region', 'us-west1',
        '--platform', 'managed',
        '--allow-unauthenticated',
        '--port', '8080'
    ]